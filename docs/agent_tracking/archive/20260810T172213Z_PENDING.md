# Pending Work

## Ordered Actions

1. **FIRST PRIORITY — Freeze the complete Stage 3 input manifest.** Generate it from the 19-row authoritative Stage 2 status, active-run pins, manifests, screening summaries, and access dispositions. Include all 16 complete work items and retain DS08, DS23, and DS28 as explicit unavailable rows. Completion requires unique work-item keys, existing/checksummed evidence paths, declared source adapters, and preserved Stage 2 roles.
2. **Build source-specific Stage 3 coverage adapters.** Extract only the observation metadata required for coverage—dataset/network, sample/station identifiers where defensible, datetime precision, coordinates, subregion, depth, method fields, variables, units, and size-domain evidence—from each pinned source without Stage 5 biomass harmonization or any PhyC inspection.
3. **Generate the full Stage 3 evidence set.** Produce temporal/cadence, seasonal effort, spatial/station, vertical, method-epoch, biological/convertibility, network-year-variable, metadata-only CMEMS-overlap, coverage-gap, and bias outputs plus reproducible figures.
4. **Apply the prespecified role gate.** Classify dataset-region-period combinations for each candidate analysis window and reference tier as eligible, secondary, exploratory, or unusable. Unknown evidence must remain unknown; access-driven absence must not become an ecological zero.
5. **Add validation and traceability.** Implement schema/key/value/missingness tests, expected-dataset assertions, generated output checksums and inventory, complete logs with `sessionInfo()`, deterministic rerun checks, documentation, and conservative stage-status reporting.
6. **Run the Stage 3 gate and reconcile the handoff.** Only after all outputs and tests pass may Stage 4 become the next active stage.

## Blockers and Missing Inputs

- DS08, DS23, and DS28 remain temporarily unavailable under the frozen access policy and must remain visible in the Stage 3 manifest with their documented consequences.
- DS28 lacks an exact machine-actionable identity and archived observation payload.
- Several complete datasets have checksum-pinned raw payloads and Stage 2 screening summaries but no common row-level interim table; Stage 3 therefore requires explicit source adapters rather than filename discovery.

## Known Warnings and Scientific Risks

- Stage 2 completion is not final scientific eligibility; Stage 3 may downgrade roles based on cadence, spatial support, method, size domain, and CMEMS-period overlap.
- Taxon or occurrence rows are not automatically independent samples, and unique moving coordinates are not automatically stations.
- Aggregator copies must not be counted as independent networks.
- Direct carbon evidence remains narrow; abundance-to-carbon convertibility and uncertainty belong to later Stage 5, while Stage 3 records whether the required inputs exist.
- DS27 requires the documented system `ncdump` bridge; failure must remain explicit rather than producing a zero-row placeholder.
- Raw files and existing acquisition pins/checksums must remain immutable.

## Deferred / Out of Scope

- Stage 4 feasibility, Stage 5 harmonization and biomass construction, outcomes, recurrence, and all later modelling remain deferred until the Stage 3 gate passes.
- CMEMS PhyC acquisition or inspection is prohibited during Stage 3.
