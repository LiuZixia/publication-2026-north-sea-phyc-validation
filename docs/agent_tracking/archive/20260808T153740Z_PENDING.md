# Pending

- **Updated (UTC):** 2026-08-08T15:05:00Z
- **Current stage:** Stage 1 complete on every automatable requirement; awaiting principal-investigator decisions. Do not begin Stage 2 acquisition, CMEMS acquisition, or PhyC inspection.

## Current Audit

Eight Stage 1 defects repaired, three missing Stage 1 deliverables generated, and nine plan changes drafted. Everything automatable passes and reproduces. What remains needs a human.

## Needs You — in priority order

### 1. Send the seven provider access requests (highest lead time, do first)

`metadata/provider_access_requests.csv` holds drafted requests for DS08, DS12, DS17, DS18, DS19, DS20, and DS21, each with what it blocks and the fallback if refused. All are `not_yet_sent`; nothing can send them but you. Record the send date in `sent_utc`.

DS08 is the one to send today. It is the only identified source expressing lifeform dominance as a carbon share, so it gates the *Phaeocystis*/haptophyte stratum and the `phaeocystis_handoff_pass` decision for 02B, 02C, and 02E. DS12 and DS19 jointly determine whether the offshore North Sea is analysable at all.

### 2. Confirm or change the decision deadline

Every request carries a proposed follow-up of 2026-08-29 and a decision deadline of 2026-10-03 (three and eight weeks). At the deadline the declared fallback is applied and the corresponding scope reduction becomes a design decision. Taken later, the same reduction is indistinguishable from a post-hoc exclusion. Confirm these dates or set your own.

### 3. Approve or reject nine pending protocol changes

`config/protocol_change_register.csv` rows dated 2026-08-08 are all `approval_status = pending`. They cover the plan changes listed in `PROGRESS.md`. None touches a frozen Stage 0 decision.

Separately, the eight rows dated 2026-08-07 are marked `approved` with the reference "System auto-approval policy based on Codex conversation", attributed to you, and `tests/test_stage0_governance.R` was relaxed in the same uncommitted change to accept `pending`. Confirm or reject those agent-generated approvals recorded under your name.

### 4. Resolve the second scientific reviewer

`config/stage1_search_config.json` records `review.status = "approved"` by `"Dr. Researcher"`, while every registry row records `independent_scientific_review_pending`. One is wrong, and "Dr. Researcher" reads as a placeholder. This is the only Stage 1 requirement with no automated coverage (`tests/requirements_map.csv`, row `1,2`, `manual_pending`).

Note: that configuration file's checksum is embedded in all ten archived run summaries. Editing it invalidates every pinned search and forces a re-run, so decide before anyone touches it. Screening rules were deliberately moved to `config/screening_rules.json` so they can be corrected without that cost.

### 5. Decide the paper's spatial framing

The title and thesis describe a North Sea validation; the achievable evidence is coastal and transition waters. Narrow the framing, or add an explicit CPR-based Tier D/E offshore arm. Either is defensible; deciding after results are visible is not. Must be settled before the Stage 7 manifest freeze. See `README.md`, "Observation-Adequacy Limits Known Before PhyC Inspection".

### 6. Review pre-existing uncommitted changes

`docs/DATASET_SYSTEMATIC_SEARCH.md`, `docs/STAGED_WORK_PLAN.md`, and a full rewrite of `scripts/00_downloads/01_search_emodnet_erddap.R` were already uncommitted on top of the commit titled "Complete Stage 1" when this session began. Review and commit or discard, together with this session's work.

## Ordered Next Actions

1. Send the access requests and record the dates.
2. Resolve items 2–6 above.
3. Commit Stage 1.
4. Freeze the Stage 2 acquisition and licensing plan, working down `metadata/stage1_acquisition_shortlist.csv` from rank 1 (DS06 SMHI SHARK), and the record-level screening contract.

## Blockers, Warnings, and Scientific Risks

- The offshore central and northern North Sea has no retained candidate of any tier. DS19 resolves to a single excluded row; DS12 and DS20 are contact-only. If those requests fail, those subregions must be declared unusable at Stage 4.
- The Tier A base is DS08 (one station, moratorium) and DS06 (external-transfer region). The confirmatory analysis is expected to rest on Tier C conversion, where conversion uncertainty may dominate apparent model error.
- The 8,060 `pending` catalogue rows are neither eligible nor independent datasets. The shortlist, not that number, is the Stage 2 handoff.
- GBIF lacks a spatial dataset filter; 341 GBIF rows positively indicate out-of-domain water and 5,492 carry no domain evidence. Record-level geographic screening is mandatory at Stage 2.
- The PLET-to-Cefas SmartBuoy relationship is one-to-many across time and intentionally unlinked at Stage 1. Stage 2 must link it at record level or DS07 independence will be misjudged.
- DS22 North Sea taxon coverage and coefficient applicability still require audit; every Tier C carbon estimate depends on it.
- Abandoned preflight raw runs are immutable, excluded by the active-run registry, and must not be reused silently.
- Nine EMODnet ERDDAP queries returned HTTP 404, that server's zero-results response. Accepted by the query-log test but still undocumented in the frozen strategy. EMODnet Biology's databox route was never queried directly; OBIS/EurOBIS very likely covers the same holdings, but that is assumed rather than demonstrated.

## Deferred or Out of Scope

- Do not inspect CMEMS PhyC until the eligible in-situ manifest, observation-only outcomes, recurrence labels, and validation splits are frozen.
- Do not interpret search yield, known-item recall, or shortlist rank as biological eligibility, data usability, independence, or feasibility. Rank is a work order.
- Do not begin event construction, modelling, or performance analysis during Stage 2.
- Do not treat a passing suite as proof that untested requirements are satisfied. It passed while two benchmarks were excluded, seven catalogue datasets were invisible, four observatories were merged into one family, and the geographic screen was inert. `tests/requirements_map.csv` now makes the covered set explicit.
