# Pending Work

## Ordered Actions

1. **FIRST PRIORITY — Replay the remaining Stage 2 provider processing scripts in bounded groups.** Use the exact existing raw files; acquisition scripts remain unexecuted during development. Completion evidence: each group verifies raw checksums and regenerates its metadata before the next group.
2. **Add an offline Stage 2 publication entry point and validation log.** Completion evidence: one `Rscript` command replays processing only, never calls an acquisition script, and passes the Stage 2 tests.
3. **Validate the generated Stage 2 inventory.** Completion evidence: the inventory reruns deterministically and `metadata/stage_file_inventory_unresolved.csv` remains empty.
4. **Add the single Stage 2 operational description and publication entry point.** Completion evidence: `docs/stages/STAGE2.md` names every script group, result contract, blocker, and handoff; one non-interactive command runs the current Stage 2 gate.
5. **Condense duplicate Stage 1/2 execution prose in the systematic-search document.** Replace detailed file-by-file operational narration with links to the stage descriptions and generated reports while preserving scientific decisions and append-only history.
6. **Run final Stage 1 and Stage 2 gates.** Completion evidence: non-interactive R commands pass, raw checksums reconcile, generated outputs reproduce, logs include session information, and tracking files match the actual worktree.
7. **Resume Stage 3 only after the consolidation gates pass.** Parse and harmonize records, deduplicate underlying observations, and audit PEG_BVOL conversion needs.

## Blockers and Missing Inputs

- DS28 LifeWatch HPLC still lacks an exact DOI, IPT resource name, or API URL; its acquisition script remains unexecuted.
- Contact-required DS17, DS18, DS19, DS20, DS21, and DS23 remain unavailable unless provider responses and usable terms arrive; current consequences must remain applied.

## Known Warnings and Scientific Risks

- The existing `data/raw/stage2/downloaded_files_inventory.md` is a historical generated Markdown artifact in immutable raw storage; it must remain immutable and be marked superseded by the tracked generated CSV inventory.
- Current Stage 2 tests pass, but remaining provider result files still need an explicit post-move replay to prove all scripts use the consolidated paths.
- Moving or renaming raw evidence could break pinned response paths and checksums; raw files will remain immutable unless a scripted, provenance-preserving migration is demonstrably required.
- Passing current tests alone is insufficient; the inventory must verify producer/output completeness and the staged gates.
- The Stage 2 gate must remain an offline replay gate; any future provider acquisition must be invoked separately and explicitly.

## Deferred / Out of Scope

- Stage 3 harmonization and biomass conversion are deferred until Stage 1/2 consolidation passes.
- CMEMS acquisition, PhyC inspection, outcome construction, modelling, dashboards, and additional papers are out of scope for this task.
- Pre-existing untracked `ds02_unique_stations.txt`, `tmp.json`, and `unique_stations.txt` are user-owned and excluded from this session.

