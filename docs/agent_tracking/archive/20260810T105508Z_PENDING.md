# Pending Work

## Ordered Actions

1. **FIRST PRIORITY — Complete record-screening for existing DS08, DS12, and DS27 evidence.** Implement real provider-format parsers and data contracts; do not use zero-row placeholders. Completion evidence: checksum-verified outputs and tests update the calculated Stage 2 status.
2. **Resolve DS28 acquisition identity before any provider call.** Completion evidence: exact DOI, IPT resource name, or API URL is registered; acquisition remains a separately authorized script invocation.
3. **Pursue remaining open/contact-only Stage 2 work items under the frozen access policy.** Completion evidence: usable provider files and terms are registered, or the generated status conservatively retains them as unavailable/not started.
4. **Start Stage 3 only after the scientific Stage 2 acquisition/screening gate is satisfied.** Parse and harmonize eligible observations, deduplicate underlying sources, and audit PEG_BVOL conversion needs.

## Blockers and Missing Inputs

- DS28 LifeWatch HPLC still lacks an exact DOI, IPT resource name, or API URL; its acquisition script remains unexecuted.
- Contact-required DS17, DS18, DS19, DS20, DS21, and DS23 remain unavailable unless provider responses and usable terms arrive; current consequences must remain applied.

## Known Warnings and Scientific Risks

- The existing `data/raw/stage2/downloaded_files_inventory.md` is a historical generated Markdown artifact in immutable raw storage; it must remain immutable and be marked superseded by the tracked generated CSV inventory.
- Current Stage 2 consolidation tests pass, but the calculated workflow still contains incomplete datasets; the offline replay gate is not scientific-stage completion.
- DS27 NetCDF files are acquired but have no authoritative row-level parser in the current R environment. The removed zero-row stub must not be restored as evidence.
- Moving or renaming raw evidence could break pinned response paths and checksums; raw files will remain immutable unless a scripted, provenance-preserving migration is demonstrably required.
- Passing current tests alone is insufficient; the inventory must verify producer/output completeness and the staged gates.
- The Stage 2 consolidation gate must remain an offline replay gate; any future provider acquisition must be invoked separately and explicitly.

## Deferred / Out of Scope

- Stage 3 harmonization and biomass conversion are deferred until the scientific Stage 2 evidence gate, not merely the completed consolidation gate, passes.
- CMEMS acquisition, PhyC inspection, outcome construction, modelling, dashboards, and additional papers are out of scope for this task.
