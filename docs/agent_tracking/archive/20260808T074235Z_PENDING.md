# Pending

- **Updated (UTC):** 2026-08-08T07:20:05Z
- **Current stage:** Stage 1 is incomplete; Stage 0 is the last passed gate. Do not begin Stage 2.

## First Priority

**Repair and execute complete, frozen searches for every required source family.**

Completion evidence:

- PLET, ICES DOME, EMODnet Biology/EurOBIS/OBIS, SMHI SHARK, PANGAEA, GBIF, `data.marine.gov.scot`, Cefas Data Hub/DASSH, and ICES Figshare have executed modules or documented reproducible export/manual routes rather than stubs;
- every search uses the prospectively frozen geography, controlled biological and measurement terms, filters, and expected fields; and
- complete immutable responses and manifests reconcile provider totals without caps, loops, partial responses, or silent endpoint failures.

## Ordered Next Actions

1. Replace the PLET, ICES DOME, Scotland CKAN, and Cefas/DASSH stubs; add the missing OBIS/EurOBIS route; verify each interface against official documentation or register a reproducible alternative.
2. Freeze source-specific query strategies containing the full domain, controlled terms and synonyms, filters, expected fields, endpoint/API version, and reviewer state.
3. Correct the Figshare search method and ICES scope; retrieve all PANGAEA results rather than stopping at 2,500 of 10,947; add explicit total, pagination-loop, cap, duplicate-page, partial-content, and terminal-page checks to every paged source.
4. Standardize manifests with scalar checksums plus exact request, response, HTTP, retry, timestamp, pagination, version, licence, and software metadata; resolve or formally register the orphaned incomplete Figshare raw run without overwriting it.
5. Download and checksum the actual pinned DS22 provider file, not only its article-metadata JSON.
6. Rebuild the append-only candidate registry with every essential Stage 1 field, direct raw-evidence links, correct provider parsing, and run-specific provenance. Do not deduplicate globally on an unqualified `dataset_id`.
7. Run the frozen known-item test for DS02, DS04, DS05, DS06, DS07, DS08, DS10, and DS16, with DS24 as cross-check; diagnose every miss.
8. Generate screening states, exclusions, duplicate-family links, canonical-provider links, and all search-flow totals from the registry.
9. Replace the current presence-based Stage 1 tests with substantive assertions for all actions, fields, quality checks, outputs, and gate criteria, then execute the full pipeline from a clean R session.

## Provisional Later Work (only after Stage 1 passes)

1. Evaluate all entries in `candidate_registry.csv` through the registered screening workflow.
   - **Completion criteria:** Add a Boolean `retained` column and a `reason` column based on the Stage 2 eligibility tests (marine, phytoplankton, within spatial boundary).
   - **Outputs:** `metadata/stage2_screening.csv`

2. Deduplicate providers and aggregators.
   - **Completion criteria:** For duplicates present in EMODnet, GBIF, and DASSH, retain only the provider's canonical source if available.
   - **Outputs:** An updated `metadata/stage2_screening.csv` with duplicates marked.

## Blockers and Risks

- DS08 access and licensing remain pending. The log says contact was initiated, but no correspondence metadata or provider response is yet recorded.
- Official API/export capabilities, authentication, service limits, and licence evidence remain unresolved for several required families.
- A second scientific reviewer check of exclusions and ambiguous source-specific query choices is not evidenced.
- The current candidate registry is not a valid search yield: it includes 10,000 unrelated Figshare records and only the first 2,500 of 10,947 PANGAEA hits.
- The current Stage 1 tests can pass while complete-source, complete-pagination, known-item, registry-schema, screening, deduplication, and flow-count requirements fail.
- `git diff --check` reports trailing whitespace in `scripts/00_downloads/01_search_emodnet_erddap.R`.
- The 2026-08-08 re-audit found no new Stage 1 evidence and reproduced every prior gate failure; the ordered actions above all remain open.

## Deferred or Out of Scope

- Do not start Stage 2 record-level screening or deduplication until the Stage 1 gate passes.
- Do not acquire or inspect CMEMS PhyC before the later in-situ manifest and observation-only split freeze.
- Do not interpret catalogue-record counts as biological observations or independent datasets.
