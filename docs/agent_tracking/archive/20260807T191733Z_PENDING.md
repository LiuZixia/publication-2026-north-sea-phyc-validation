# Pending

- **Updated (UTC):** 2026-08-07T18:04:01Z
- **Carried forward after milestone snapshot:** `20260807T180401Z`

## First Priority

**Obtain accountable scientific approval for the five material Stage 0 protocol decisions.**

Required input:

- the reviewer's real accountable name; and
- a traceable approval reference, such as a dated email, meeting-minutes identifier, issue/decision-record link, or an explicit named approval statement from the repository owner with its UTC date.

Completion evidence:

- every row in `config/protocol_change_register.csv` has the real reviewer name, `approval_status=approved`, and the supplied approval reference;
- `Rscript scripts/00_setup.R` reports all 83 assertions passing with zero warnings and zero skips;
- the successful full-pipeline log and final derived checksums are recorded in `PROGRESS.md`;
- a Stage 0 completion snapshot is created under `docs/agent_tracking/archive/`; and
- all Stage 0 files and snapshots are committed coherently before Stage 1 begins.

## Ordered Next Actions

1. Receive the reviewer identity and approval reference from the user or scientific team.
2. Record that evidence without altering the already frozen scientific choices.
3. Run `Rscript scripts/00_setup.R` and require 83/83 passing assertions.
4. Reconcile tracking, create the Stage 0 gate-completion snapshot, and commit the completed gate.

## Blockers and Missing Inputs

- The repository does not identify an accountable scientific reviewer or contain a traceable approval reference.
- This evidence cannot be inferred from Git authorship or manufactured by an AI agent.

## Known Warnings and Scientific Risks

- The technical pipeline passes 81 assertions with zero warnings/skips, but Stage 0 remains open until the two approval assertions pass.
- The local branch is two commits ahead of `origin/main`; no push is required for the local Stage 0 gate, and remote availability has not been checked.

## Deferred or Out of Scope

- Do not execute Stage 1 searches or record screening until Stage 0 passes.
- Do not acquire or inspect CMEMS PhyC before the observation-side manifest and splits are frozen.
- Do not infer taxonomic information from total PhyC.
- Do not claim paper results or biological conclusions before the complete held-out pipeline runs from registered inputs.
