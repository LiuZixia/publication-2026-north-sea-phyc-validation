# Pending Work

## Ordered Actions

1. **FIRST PRIORITY — Consolidate Stage 1.** Add one concise description, stage-owned script entry points/subfolders, organized generated metadata, and a corrected deterministic validator while preserving append-only raw evidence and existing scientific decisions. Completion evidence: clean-session Stage 1 execution and validation pass, with deterministic inventory regeneration.
2. **Consolidate Stage 2 in small dataset batches.** Add one concise description, stage-owned script entry points/subfolders, organized generated results, and a complete inventory. Completion evidence: each affected acquisition/screening batch reruns or verifies immutable downloads and passes before the next batch.
3. **Close the DS10 legacy PLET provenance gap.** Add a version-controlled exact-URL acquisition or scripted intake that verifies the existing registered checksum without rewriting raw evidence. Completion evidence: the generated unresolved inventory has zero rows.
4. **Reconcile shared metadata.** Leave only genuinely cross-stage registries at `metadata/`; route stage-owned generated files into stage result folders through scripts. Completion evidence: generated ownership audit has no ambiguous Stage 1/2 artifacts.
5. **Run final Stage 1 and Stage 2 gates.** Completion evidence: non-interactive R commands pass, raw checksums reconcile, generated outputs reproduce, logs include session information, and tracking files match the actual worktree.
6. **Resume Stage 3 only after the consolidation gates pass.** Parse and harmonize records, deduplicate underlying observations, and audit PEG_BVOL conversion needs.

## Blockers and Missing Inputs

- DS28 LifeWatch HPLC still lacks an exact DOI, IPT resource name, or API URL; its acquisition script remains unexecuted.
- Contact-required DS17, DS18, DS19, DS20, DS21, and DS23 remain unavailable unless provider responses and usable terms arrive; current consequences must remain applied.

## Known Warnings and Scientific Risks

- The existing `data/raw/stage2/downloaded_files_inventory.md` is a generated Markdown artifact in immutable raw storage; its completeness, producer, and suitability as a publication inventory must be audited.
- Stage 1 and Stage 2 outputs are currently interleaved in top-level `metadata/`, obscuring producer ownership.
- Moving or renaming raw evidence could break pinned response paths and checksums; raw files will remain immutable unless a scripted, provenance-preserving migration is demonstrably required.
- Passing current tests alone is insufficient; the inventory must verify producer/output completeness and the staged gates.
- Existing Stage 1 and Stage 2 test expectations are stale relative to the current generated provider-request and acquisition-status registries; repairs must assert calculated states, not copy new literal counts.

## Deferred / Out of Scope

- Stage 3 harmonization and biomass conversion are deferred until Stage 1/2 consolidation passes.
- CMEMS acquisition, PhyC inspection, outcome construction, modelling, dashboards, and additional papers are out of scope for this task.
- Pre-existing untracked `ds02_unique_stations.txt`, `tmp.json`, and `unique_stations.txt` are user-owned and excluded from this session.
