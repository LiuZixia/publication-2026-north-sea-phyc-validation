# Pending

- **Updated (UTC):** 2026-08-07T17:31:48Z
- **Carried forward after session snapshot:** `20260807T173148Z`

## First Priority

**Close the remaining Stage 0 governance and reproducibility requirements before beginning Stage 1.**

Completion evidence:

- an identifiable accountable scientific reviewer and traceable approval record cover the domain roles, removal of the coastline mask, 5.5 km coastal rule, and identifier amendments;
- one exact station-assignment rule is frozen consistently in configuration, code, protocol register, and documentation;
- tests cover the default 5.5 km threshold on both sides, boundary and equal-distance behavior, centroid tie-breaking where applicable, missing/invalid coordinates, the non-overlapping full domain partition, manifest fields, response-count/completeness reconciliation, raw checksums, and derived-output provenance;
- the spatial acquisition step detects transfer limits/server caps, records the evidence behind license and version values, and does not merely hardcode `complete`;
- raw acquisition is restartable and idempotent, and spatial outputs can be regenerated from an explicitly selected registered immutable raw run without forcing a fresh download;
- the full Stage 0 entry point and narrow tests pass from the locked environment; and
- the corrected milestone, tracking snapshots, tests, and generated outputs are committed with a clean reconciled worktree.

## Ordered Next Actions

1. Obtain and register genuine scientific approval; replace `Dr. Principal Investigator` only with an actual accountable reviewer identity and a traceable approval reference.
2. Resolve `0.05deg` versus `5500 m`, then define deterministic boundary and equal-distance behavior in the frozen rule.
3. Expand the Stage 0 tests to cover the full gate, including partition and raw/derived provenance assertions.
4. Make spatial acquisition completeness checks explicit and verifiable, including returned-versus-reported counts or equivalent server-cap evidence and a source for license/version metadata.
5. Separate acquisition from derivation or add a registered-input mode so unchanged verified raw files are reused idempotently.
6. Run the complete Stage 0 pipeline from the locked environment, reconcile artifacts and tracking, create the milestone snapshot, and commit the passed gate before Stage 1.

## Blockers and Missing Inputs

- Accountable scientific reviewer identity and traceable approval evidence are not present in the repository.
- The intended exact unit and deterministic semantics of the coastal buffer/boundary rule require a scientific decision.

## Known Warnings and Scientific Risks

- The current 33-pass suite is narrower than the Stage 0 gate and must not be reported as proof of full compliance.
- The current manifest's `complete` and license fields are assertions rather than fully evidenced checks.
- Always downloading current provider responses during setup makes repeated builds dependent on changing external state.
- The local branch is two commits ahead of `origin/main`; remote review availability has not been verified.

## Deferred or Out of Scope

- Do not execute Stage 1 searches or record screening until Stage 0 passes.
- Do not acquire or inspect CMEMS PhyC before the observation-side manifest and splits are frozen.
- Do not infer taxonomic information from total PhyC.
- Do not claim paper results or biological conclusions before the complete held-out pipeline runs from registered inputs.
