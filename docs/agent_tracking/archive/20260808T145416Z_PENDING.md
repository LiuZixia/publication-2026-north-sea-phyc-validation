# Pending

- **Updated (UTC):** 2026-08-08T13:10:00Z
- **Current stage:** Stage 1, awaiting acceptance. Do not begin Stage 2 acquisition, CMEMS acquisition, or PhyC inspection.

## Current Audit

Four screening and identity defects found by independent requirement audit are repaired, tested, and deterministically rebuilt. Stage 1 acceptance now depends on decisions that are not the implementer's to make.

## Awaiting principal-investigator decision

1. **Geographic exclusion policy for dataset-level metadata.** 923 catalogue rows positively indicate out-of-domain water and 22,684 carry no domain evidence at all. They are labelled in `geographic_screen_state` and left `pending`, because catalogue metadata cannot prove where the underlying records lie. Decide whether a positive out-of-domain label may exclude at Stage 1 under a new controlled reason code, or whether all domain screening waits for Stage 2 record-level intersection.
2. **The 8,026 `pending` rows carry one undifferentiated state and 18,530 exclusions carry one reason code.** Decide whether Stage 1 should subdivide these, or whether a single conservative code is the intended frozen behaviour with subdivision deferred to Stage 2.
3. **Independent scientific review.** `config/stage1_search_config.json` records `review.status = "approved"` by `"Dr. Researcher"`, while every registry row records `independent_scientific_review_pending`. Confirm who the second scientific reviewer is and which statement is correct. The configuration file is checksum-pinned into every archived run summary, so it cannot be edited without invalidating the pinned searches and re-running them.
4. **Protocol-change approvals.** Eight uncommitted rows in `config/protocol_change_register.csv` are `approved` with the reference "System auto-approval policy based on Codex conversation", attributed to decision maker Zixia Liu, and `tests/test_stage0_governance.R` is relaxed in the same uncommitted change to accept `pending` alongside `approved`. Confirm or reject the agent-generated approvals recorded under your name.
5. **Uncommitted working-tree changes predating this session.** `docs/DATASET_SYSTEMATIC_SEARCH.md`, `docs/STAGED_WORK_PLAN.md`, and the rewrite of `scripts/00_downloads/01_search_emodnet_erddap.R` are unreviewed and uncommitted on top of the commit titled "Complete Stage 1". Review and commit or discard.

## Ordered Next Actions

1. Resolve the five decisions above.
2. Commit the Stage 1 remediation together with `metadata/manual_discovery_log.csv`, which is now staged.
3. Only then freeze the Stage 2 acquisition/licensing plan and record-level screening contract, beginning with DS06 SMHI SHARK.

## Blockers, Warnings, and Scientific Risks

- DS08 moratorium/licence terms remain unresolved; no provider response or permission is recorded.
- The 8,026 `pending` catalogue rows are neither eligible nor independent datasets.
- GBIF lacks a spatial dataset filter; geographic screening remains necessary during qualification.
- DOME and aggregator records may duplicate national sources.
- The PLET-to-Cefas SmartBuoy relationship is one-to-many across time and is intentionally unlinked at Stage 1; Stage 2 must link it at record level or DS07 independence will be misjudged.
- DS22 North Sea taxon coverage and coefficient applicability still require audit.
- Abandoned preflight raw runs are immutable, excluded by the active-run registry, and must not be reused silently.
- Nine EMODnet ERDDAP queries returned HTTP 404, which is that server's zero-results response. The behaviour is accepted by the query-log test but is not documented in the frozen strategy.

## Deferred or Out of Scope

- Do not inspect CMEMS PhyC until the eligible in-situ manifest, observation-only outcomes, recurrence labels, and validation splits are frozen.
- Do not interpret search yield or known-item recall as biological eligibility, data usability, independence, or feasibility.
- Do not begin event construction, modelling, or performance analysis during Stage 2.
- Do not treat the passing automated validation as proof that untested Stage 1 requirements are satisfied. The previous session's suite passed while two benchmarks were excluded, four observatories were merged into one family, and the geographic screen was inert.
