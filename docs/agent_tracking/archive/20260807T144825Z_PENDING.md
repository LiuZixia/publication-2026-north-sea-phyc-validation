# Pending

- **Updated (UTC):** 2026-08-07T14:42:42Z
- **Carried forward after session snapshot:** `20260807T144242Z`

## First Priority

Generate the candidate registry and systematic-search flow counts from archived API evidence, and add the remaining in-situ source acquisition scripts.

Completion evidence:
- Implement scripts for remaining API sources (e.g., PLET, ICES DOME, SMHI SHARK).
- A script to combine all downloaded search manifests into a single candidate registry.
- Generated search-flow diagram counts based on the combined registry.

## Ordered Next Actions

1. Implement acquisition scripts for remaining APIs (PLET, ICES DOME, SMHI SHARK).
2. Create `scripts/01_build_candidate_registry.R` to parse and normalize the raw API responses.
3. Add provider-specific screening tests and `scripts/02_screen_and_deduplicate.R`.
4. Advance through temporal/spatial coverage auditing only after the search and acquisition gates pass.
5. Create dataset acquisition scripts (e.g., `02_download_insitu_*.R`) based on the deduplicated registry.

## Blockers and Missing Inputs

- Exact API capabilities, authentication requirements, and rate limits still need to be verified from official documentation for the remaining providers.
- The exact CMEMS product and dataset ID must be frozen from official metadata before any PhyC values are acquired.
- Restricted/contact-only datasets remain unavailable until providers grant access and terms are recorded.

## Known Risks and Required Follow-Up

- Prevent duplicate national records exposed through multiple aggregators from being counted as independent evidence.
- Do not let catalogue descriptions substitute for record-level acquisition and method screening.
- Keep credentials out of scripts, manifests, logs, and Git history.
- Freeze observation eligibility and grouped validation design before extracting or inspecting PhyC values.

## Deferred or Out of Scope

- No manual raw-file placement or unrecorded browser download.
- No CMEMS extraction before the in-situ manifest and product selection are frozen.
- No manuscript result, performance metric, or biological conclusion until calculated by the R pipeline.
- No taxonomic identification claim from total PhyC.
