# Pending

- **Updated (UTC):** 2026-08-08T09:39:50Z
- **Current stage:** Stage 1 remediation is required before Stage 2. Do not begin CMEMS acquisition or PhyC inspection.

## Current Audit

The fresh deterministic rebuild and test suite pass, but a manual audit found mandatory Stage 1 scope, strategy, and duplicate-lineage gaps not asserted by the tests.

## First Priority

**Repair and revalidate the Stage 1 search scope, frozen source strategies, and cross-catalogue duplicate links before Stage 2 begins.**

Completion evidence:

- global or otherwise unfiltered catalogues are reproducibly screened to the frozen North Sea geography, with out-of-domain examples excluded rather than left pending;
- each source strategy records the required terms/synonyms, filters, expected response fields, and matches the implemented query; affected searches are rerun append-only after the corrected freeze;
- known provider/aggregator catalogue copies, including the SmartBuoy PLET, OBIS/DASSH, and Cefas records, link to a defensible canonical family rather than relying only on exact DOI equality;
- tests assert these scope and lineage conditions; and
- the compiler and full validation pass from the corrected pinned runs with reconciled flow counts.
- the Stage 1 implementation, frozen configuration, tests, and registries are committed or otherwise genuinely version-controlled as one reviewable change set.

## Ordered Next Actions

1. Apply the geographic strategy promised for GBIF and audit other source-specific searches against every frozen term and filter.
2. Expand catalogue-family linkage beyond exact DOI equality using evidence-backed identifiers and known duplicate benchmarks.
3. Add the missing source-strategy detail and reconcile configuration descriptions with actual query implementations; rerun affected searches append-only.
4. Add tests for out-of-domain rejection and known cross-catalogue duplicate links, then rerun the complete Stage 1 validation.
5. Reconcile the complete dirty worktree and place the Stage 1 implementation, configuration, tests, and registries under version control without absorbing unrelated changes.
6. Obtain independent scientific review of ambiguous query/exclusion choices where available, and retain an explicit pending state if genuinely unavailable.
7. After Stage 1 passes in full, freeze the Stage 2 acquisition/licensing plan and record-level screening contract, beginning with DS06 SMHI SHARK.

## Blockers, Warnings, and Scientific Risks

- DS08 moratorium/licence terms remain unresolved; no provider response or permission is recorded.
- Independent scientific review of Stage 1 ambiguous choices remains pending and must not be described as approval.
- The frozen GBIF strategy says geography terms will be applied to archived metadata, but the compiler currently marks all GBIF query hits as relevant; clearly out-of-domain records remain `pending`.
- Cross-catalogue deduplication currently uses exact normalized DOI only; known SmartBuoy catalogue copies remain separate canonical families.
- The frozen strategies do not enumerate source-specific synonyms and expected response fields, and some descriptions do not match the implementation exactly.
- Stage 1 search-run directories have machine-readable manifests and summaries but no per-run human-readable acquisition logs or acquisition-time package-version record as required by the repository provenance rules.
- The substantive Stage 1 helpers, scripts, frozen configuration, tests, and registries are currently untracked in Git, so they do not yet meet the requirement to be version-controlled and reviewable.
- The 7,919 `pending` catalogue rows are neither eligible nor independent datasets.
- GBIF lacks a spatial dataset filter; geographic screening remains necessary.
- DOME and aggregator records may duplicate national sources.
- DS22 North Sea taxon coverage and coefficient applicability still require audit.
- Abandoned preflight raw runs are immutable, excluded by the active-run registry, and must not be reused silently.

## Deferred or Out of Scope

- Do not inspect CMEMS PhyC until the eligible in-situ manifest, observation-only outcomes, recurrence labels, and validation splits are frozen.
- Do not interpret search yield or known-item recall as biological eligibility, data usability, independence, or feasibility.
- Do not begin event construction, modelling, or performance analysis during Stage 2.
- Do not treat the passing automated validation as proof that untested Stage 1 requirements are satisfied.
